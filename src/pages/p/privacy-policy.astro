---
import ContentPage from "@/components/ContentPage.astro";
import content from "@/data/content.json";
const page = content.find((item) => item.path === "/p/privacy-policy.html");
if (!page) throw new Error("Missing imported page: /p/privacy-policy.html");
---
<ContentPage page={page} />
