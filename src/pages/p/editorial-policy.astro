---
import ContentPage from "@/components/ContentPage.astro";
import content from "@/data/content.json";
const page = content.find((item) => item.path === "/p/editorial-policy.html");
if (!page) throw new Error("Missing imported page: /p/editorial-policy.html");
---
<ContentPage page={page} />
